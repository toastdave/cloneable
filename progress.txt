- Task completed: US-002 add Start/Stop UI controls.
- Decisions: keep frontend-only state toggle; defer Tauri invoke wiring to US-003; bespoke CSS to make status obvious.
- Files changed: src/App.tsx, src/App.css, PRD.jsonc.
- Blockers/notes: backend start/stop not wired yet; no persistence yet.

- Task completed: US-001 verify scaffold builds with Bun.
- Decisions: ran `bun install` + `bun run build` to cover install and typecheck/build; skipped `bun run tauri dev` due to headless run.
- Files changed: PRD.jsonc, progress.txt.
- Blockers/notes: re-run `bun run tauri dev` on desktop to confirm window launch.

- Task completed: US-003 Tauri start/stop command bridge.
- Decisions: keep state in app-managed Mutex; use timestamp-based session id; surface errors in UI instead of silent failures.
- Files changed: src-tauri/src/lib.rs, src/App.tsx, src/App.css, PRD.jsonc, progress.txt.
- Blockers/notes: stop returns session id only; recording persistence wired in US-009.

- Task completed: US-004 global mouse click capture in Rust backend.
- Decisions: use rdev global listener; capture coordinates from latest mouse move; buffer via crossbeam channel; filter events by is_recording in worker thread.
- Files changed: src-tauri/src/lib.rs, src-tauri/Cargo.toml, PRD.jsonc, progress.txt.
- Blockers/notes: OS permissions (e.g., macOS accessibility) may still be required for global input capture.

- Task completed: US-005 global keyboard entry capture.
- Decisions: reuse existing rdev listener to avoid multiple global hooks; record key Debug string (no text event in rdev 0.5.3); buffer via channel to keep hook thread light.
- Files changed: src-tauri/src/lib.rs, PRD.jsonc, progress.txt.
- Blockers/notes: printable text capture not available with current rdev version; richer text reconstruction deferred.

- Task completed: US-006 full-screen screenshot per event.
- Decisions: use `screenshots` crate with primary screen capture; store PNGs under app data `recordings/<session>/shots` and attach path/error to each event; keep capture in worker thread to avoid blocking hook callback.
- Files changed: src-tauri/src/lib.rs, src-tauri/Cargo.toml, PRD.jsonc, progress.txt.
- Blockers/notes: capture is best-effort and tied to primary screen only; screenshot crate has future-incompatibility warning noted by cargo.

- Task completed: US-007 click crop screenshot per event.
- Decisions: fixed crop size 400x300 centered on click; clamp to screen bounds; capture from primary screen; store path/error on click event.
- Files changed: src-tauri/src/lib.rs, src-tauri/Cargo.toml, PRD.jsonc, progress.txt.
- Blockers/notes: crop uses another screen capture per click; multi-monitor coordinates not mapped yet.

- Task completed: US-008 window crop screenshot per event (best-effort).
- Decisions: implement window image capture per event with explicit fallback to a full-screen capture; mark windowCropFallback=true on fallback to satisfy 3-image requirement without OS-specific window bounds.
- Files changed: src-tauri/src/lib.rs, PRD.jsonc, progress.txt.
- Blockers/notes: active window bounds lookup not implemented yet, so window image is always fallback; replace with OS-specific bounds when available.

- Task completed: US-009 persist recording session to local filesystem.
- Decisions: write recording.json with click/key event arrays + timestamps; save via temp file then rename for atomicity; block starting a new recording if previous session is unsaved.
- Files changed: src-tauri/src/lib.rs, PRD.jsonc, progress.txt.
- Blockers/notes: if save fails, stop returns error and keeps session data in memory; retry stop to re-save after fixing filesystem issue.

- Task completed: US-016 README and walkthrough deliverables.
- Decisions: keep README concise; document shipped vs cut scope; use written walkthrough to avoid video requirement; include AI usage notes explicitly.
- Files changed: README.md, PRD.jsonc, progress.txt.
- Blockers/notes: none.

- Task completed: US-010 load completed recording for review.
- Decisions: add load_recording Rust command to read recording.json; build chronological step list in UI on stop; show per-step metadata and screenshot paths without live streaming.
- Files changed: src-tauri/src/lib.rs, src/App.tsx, src/App.css, PRD.jsonc, progress.txt.
- Blockers/notes: step model is display-only; richer grouping/annotations still pending (US-011/US-012).

- Task completed: US-011 parse events into discernible steps.
- Decisions: define a backend Step model with consistent fields; build steps from click/key events on stop and backfill on load if missing; keep UI rendering step-based with generic labels to avoid over-inference.
- Files changed: src-tauri/src/lib.rs, src/App.tsx, PRD.jsonc, progress.txt.
- Blockers/notes: steps do not yet include annotation or actionType; follow-ups in US-012/US-013.

- Task completed: US-012 annotate steps with title and description.
- Decisions: store title/description on Step model with serde defaults; add a Tauri command to update annotations and rewrite recording.json; update UI detail panel with editable fields and save button while preserving screenshot paths.
- Files changed: src-tauri/src/lib.rs, src/App.tsx, src/App.css, PRD.jsonc, progress.txt.
- Blockers/notes: actionType classification still pending (US-013).

- Task completed: US-013 classify action type per step.
- Decisions: add ActionType enum on steps with defaults inferred from event type; UI exposes dropdown override and persists via existing update command; keep wait/assert manual-only.
- Files changed: src-tauri/src/lib.rs, src/App.tsx, src/App.css, PRD.jsonc, progress.txt.
- Blockers/notes: none.
